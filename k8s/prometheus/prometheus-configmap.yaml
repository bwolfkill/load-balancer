apiVersion: v1
kind: ConfigMap
metadata:
  name: prometheus-config
data:
  prometheus.yaml: |
    global:
      scrape_interval: 5s
    scrape_configs:
      - job_name: loadbalancer
        static_configs:
          - targets: ['loadbalancer:8080']
        metrics_path: /metrics/prometheus
